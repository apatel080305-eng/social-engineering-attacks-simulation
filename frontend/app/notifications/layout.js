export const metadata = {
  title: "Notifications",
  description: "Manage your platform alerts and broadcast history on Topic AI.",
};

export default function NotificationsLayout({ children }) {
  return <>{children}</>;
}
