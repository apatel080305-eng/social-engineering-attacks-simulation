export const metadata = {
  title: "Account Settings",
  description: "Secure and manage your Topic AI profile, security settings, and multi-factor authentication.",
};

export default function SettingsLayout({ children }) {
  return <>{children}</>;
}
